name: Restore Deleted Files and Clean Up

on:
  workflow_dispatch:  # Avvio manuale

jobs:
  restore-and-clean:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Necessario per ripristinare file da vecchi commit

      - name: Restore deleted files from commit 7894e1d99ca2bd83fe51695000f08593cd7cd202
        run: |
          git checkout 7894e1d99ca2bd83fe51695000f08593cd7cd202 -- logos/
          git status  # Controllo per vedere i file ripristinati

      - name: Rename files with double extensions
        run: |
          find . -type f -name "*.png.png" | while read file; do
            mv "$file" "${file%.png.png}.png"
          done

      - name: Remove files with parentheses in the name
        run: |
          find . -type f \( -name "*(*" -o -name "*)*" \) -delete

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add .
          git commit -m "Restored deleted files, renamed .png.png files, and removed files with parentheses" || echo "No changes to commit"
          git push || echo "No changes to push"
